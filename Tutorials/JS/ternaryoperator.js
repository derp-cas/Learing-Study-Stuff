function checkEqual(a, b) {
    return a === b ? "Equal" : "Not Equal";
}

checkEqual(1, 2);

//multiple conditional Ternary opearators
function checkSign(num) {
    return num < 0 ? "negative" : num > 0 ? "positive" : "zero";
}

checkSign(10);
